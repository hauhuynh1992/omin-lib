<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" android:exitFadeDuration="@android:integer/config_mediumAnimTime">
    <!-- Focused and not pressed -->
    <item android:drawable="@drawable/button_cart_focus" android:state_focused="true" android:state_pressed="false" />

    <!-- Focused and pressed -->
    <item android:drawable="@drawable/button_cart_focus" android:state_focused="true" android:state_pressed="true" />

    <!-- Focused and pressed -->
    <item android:drawable="@drawable/button_cart_selected" android:state_selected="true" />

    <!-- Unfocused and pressed -->
    <!-- Default color -->
    <item android:drawable="@drawable/button_cart_lose_focus" />
</selector>